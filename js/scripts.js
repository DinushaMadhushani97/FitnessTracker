// scripts.js

// Function to navigate to a specific page
function navigateTo(page) {
    window.location.href = page;
}

// Example of adding more interactivity if needed in the future
document.addEventListener('DOMContentLoaded', function() {
    // Any additional JavaScript logic can be added here
    console.log("Fitness Tracker website is ready!");
});
